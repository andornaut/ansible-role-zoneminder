---
zoneminder_docker_image: andornaut/zoneminder
zoneminder_docker_volumes:
  events: /var/docker-volumes/zoneminder-events
  images: /var/docker-volumes/zoneminder-images
  mysql: /var/docker-volumes/zoneminder-mysql
zoneminder_port: 8080
zoneminder_shm_size: 512m