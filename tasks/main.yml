---
- name: Start Zoneminder Docker container
  docker_service:
    project_name: zoneminder
    definition:
      version: "2"
      services:
        zoneminder:
          container_name: zoneminder
          image: "{{ zoneminder_docker_image }}"
          ports:
            - "{{ zoneminder_port }}:80"
          restart: always
          tmpfs: /tmp/zoneminder-tmpfs
          volumes:
            - "{{ docker_volumes.zoneminder_events }}:/var/cache/zoneminder/events"
            - "{{ docker_volumes.zoneminder_images }}:/var/cache/zoneminder/images"
            - "{{ docker_volumes.zoneminder_mysql }}:/var/lib/mysql"
